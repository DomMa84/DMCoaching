---
// src/layouts/Layout.astro v1.2.9 (Admin CSS integriert)
// CSS-System v1.2.9 - Admin Dashboard Styles hinzugefügt
// ✅ ÄNDERUNGEN v1.2.9:
// - Admin Dashboard CSS-Klassen hinzugefügt
// - Konsistente Admin-Komponenten
// - Modal-System für Admin
// - Table-Styles für Kontakt-Management
// - Form-Styles für Admin-Interface

export interface Props {
  title: string;
  description?: string;
}

const { title, description = "Dominik Maier - Coaching & Interim Management" } = Astro.props;

console.log('🎨 Layout v1.2.9 loaded - Admin CSS integriert');
---

<!DOCTYPE html>
<html lang="de" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- AOS CSS -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  </head>
  <body class="font-montserrat bg-white dark:bg-gray-900 text-gray-900 dark:text-white transition-colors duration-300">
    <slot />
    
    <!-- AOS JS -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
      // Safe AOS initialization
      if (typeof AOS !== 'undefined') {
        try {
          AOS.init({
            duration: 800,
            once: true,
            offset: 100
          });
          console.log('✅ AOS initialized successfully v1.2.9');
        } catch (error) {
          console.log('❌ AOS initialization failed:', error);
        }
      } else {
        console.log('⚠️ AOS not loaded');
      }
    </script>
    
    <!-- Theme Toggle Script -->
    <script>
      // Theme toggle functionality
      function initTheme() {
        const themeToggleDarkIcon = document.getElementById('theme-toggle-dark-icon');
        const themeToggleLightIcon = document.getElementById('theme-toggle-light-icon');
        
        // Change the icons inside the button based on previous settings
        if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
          if (themeToggleLightIcon) themeToggleLightIcon.classList.remove('hidden');
          if (themeToggleDarkIcon) themeToggleDarkIcon.classList.add('hidden');
        } else {
          document.documentElement.classList.remove('dark');
          if (themeToggleDarkIcon) themeToggleDarkIcon.classList.remove('hidden');
          if (themeToggleLightIcon) themeToggleLightIcon.classList.add('hidden');
        }

        const themeToggleBtn = document.getElementById('theme-toggle');
        if (themeToggleBtn) {
          themeToggleBtn.addEventListener('click', function() {
            // Toggle icons inside button
            if (themeToggleDarkIcon) themeToggleDarkIcon.classList.toggle('hidden');
            if (themeToggleLightIcon) themeToggleLightIcon.classList.toggle('hidden');

            // If set via local storage previously
            if (localStorage.getItem('theme')) {
              if (localStorage.getItem('theme') === 'light') {
                document.documentElement.classList.add('dark');
                localStorage.setItem('theme', 'dark');
              } else {
                document.documentElement.classList.remove('dark');
                localStorage.setItem('theme', 'light');
              }
            } else {
              if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.setItem('theme', 'light');
              } else {
                document.documentElement.classList.add('dark');
                localStorage.setItem('theme', 'dark');
              }
            }
          });
        }
      }

      // Initialize theme on load
      document.addEventListener('DOMContentLoaded', initTheme);
    </script>

    <style>
      /* ✅ CSS-SYSTEM v1.2.9 - ADMIN CSS INTEGRIERT */
      
      /* === CORE SYSTEM (BEWÄHRT) === */
      body {
        font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, sans-serif;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
      }

      /* === RESPONSIVE BREAKPOINTS === */
      @media (min-width: 640px) {
        .sm\:grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
        .sm\:text-5xl { font-size: 3rem; line-height: 1; }
      }

      @media (min-width: 768px) {
        .md\:grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
        .md\:grid-cols-3 { grid-template-columns: repeat(3, 1fr); }
        .md\:text-6xl { font-size: 3.75rem; line-height: 1; }
      }

      @media (min-width: 1024px) {
        .lg\:grid-cols-4 { grid-template-columns: repeat(4, 1fr); }
        .lg\:flex-row { flex-direction: row; }
        .lg\:justify-between { justify-content: space-between; }
        .lg\:items-center { align-items: center; }
        .lg\:mb-0 { margin-bottom: 0; }
      }

      /* === PRIMARY GOLD SYSTEM === */
      .text-primary-gold { color: #D2AE6C; }
      .bg-primary-gold { background-color: #D2AE6C; }
      .bg-primary-gold-10 { background-color: rgba(210, 174, 108, 0.1); }
      .bg-primary-gold-20 { background-color: rgba(210, 174, 108, 0.2); }
      .bg-primary-gold-30 { background-color: rgba(210, 174, 108, 0.3); }
      .border-primary-gold { border-color: #D2AE6C; }
      .hover\:bg-primary-gold:hover { background-color: #D2AE6C; }
      .hover\:text-primary-gold:hover { color: #D2AE6C; }

      /* === HERO OVERLAY SYSTEM === */
      .hero-text-simple-white {
        color: white;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
      }

      /* === VALUE CARDS SYSTEM === */
      .value-tile-improved {
        height: 420px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      @media (min-width: 1024px) {
        .value-tile-improved {
          height: 480px;
        }
      }

      /* === PROCESS STEP SYSTEM === */
      .process-step-box {
        height: 180px;
      }

      @media (min-width: 768px) {
        .process-step-box {
          height: 200px;
        }
      }

      @media (min-width: 1024px) {
        .process-step-box {
          height: 220px;
        }
      }

      /* === DESKTOP GRID SYSTEM === */
      .services-desktop-grid,
      .value-desktop-grid,
      .process-desktop-grid {
        display: grid !important;
        grid-template-columns: repeat(4, 1fr) !important;
        gap: 2rem !important;
      }

      @media (max-width: 1023px) {
        .services-desktop-grid,
        .value-desktop-grid,
        .process-desktop-grid {
          grid-template-columns: repeat(2, 1fr) !important;
        }
      }

      @media (max-width: 767px) {
        .services-desktop-grid,
        .value-desktop-grid,
        .process-desktop-grid {
          grid-template-columns: 1fr !important;
        }
      }

      /* === UTILITIES === */
      .w-half { width: 50%; }
      .min-h-80vh { min-height: 80vh; }
      .rotate-neg-90 { transform: rotate(-90deg); }

      /* ================================ */
      /* === ADMIN DASHBOARD CSS v1.2.9 === */
      /* ================================ */

      /* === ADMIN ICONS === */
      .admin-icon {
        font-size: 3rem;
        line-height: 1;
      }

      /* === ADMIN INPUTS === */
      .admin-input {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 1px solid #E5E7EB;
        border-radius: 0.5rem;
        background-color: #FFFFFF;
        color: #1F2937;
        font-size: 0.875rem;
        transition: border-color 0.3s ease;
      }

      .dark .admin-input {
        border-color: #374151;
        background-color: #1F2937;
        color: #F9FAFB;
      }

      .admin-input:focus {
        outline: none;
        border-color: #D2AE6C;
        box-shadow: 0 0 0 3px rgba(210, 174, 108, 0.1);
      }

      .admin-input:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }

      /* === ADMIN BUTTONS === */
      .admin-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 0.5rem;
        font-size: 0.875rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        gap: 0.5rem;
      }

      .admin-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }

      .admin-btn-primary {
        background-color: #D2AE6C;
        color: white;
      }

      .admin-btn-primary:hover:not(:disabled) {
        background-color: #B8941F;
        transform: translateY(-1px);
      }

      .admin-btn-secondary {
        background-color: #6B7280;
        color: white;
      }

      .admin-btn-secondary:hover:not(:disabled) {
        background-color: #4B5563;
      }

      .admin-btn-link {
        background: none;
        color: #D2AE6C;
        padding: 0.5rem;
        font-size: 0.875rem;
        border: none;
        cursor: pointer;
        text-decoration: none;
      }

      .admin-btn-link:hover {
        color: #B8941F;
        background-color: rgba(210, 174, 108, 0.1);
        border-radius: 0.25rem;
      }

      .admin-btn-close {
        background: none;
        border: none;
        font-size: 1.5rem;
        color: #6B7280;
        cursor: pointer;
        padding: 0.5rem;
        border-radius: 0.25rem;
        transition: all 0.2s ease;
      }

      .admin-btn-close:hover {
        background-color: #F3F4F6;
        color: #374151;
      }

      .dark .admin-btn-close:hover {
        background-color: #374151;
        color: #E5E7EB;
      }

      /* === ADMIN ALERTS === */
      .admin-alert {
        padding: 1rem;
        border-radius: 0.5rem;
        margin-bottom: 1rem;
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
      }

      .admin-alert-info {
        background-color: #EFF6FF;
        border: 1px solid #DBEAFE;
        color: #1E40AF;
      }

      .dark .admin-alert-info {
        background-color: rgba(30, 64, 175, 0.2);
        border-color: #1E40AF;
        color: #93C5FD;
      }

      .admin-alert-error {
        background-color: #FEF2F2;
        border: 1px solid #FECACA;
        color: #B91C1C;
      }

      .dark .admin-alert-error {
        background-color: rgba(127, 29, 29, 0.2);
        border-color: #991B1B;
        color: #FCA5A5;
      }

      /* === ADMIN SPINNER === */
      .admin-spinner {
        width: 1rem;
        height: 1rem;
        border: 2px solid transparent;
        border-top: 2px solid currentColor;
        border-radius: 50%;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
      }

      /* === ADMIN STATISTICS GRID === */
      .admin-stats-grid {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: 1fr;
      }

      @media (min-width: 640px) {
        .admin-stats-grid {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (min-width: 1024px) {
        .admin-stats-grid {
          grid-template-columns: repeat(4, 1fr);
        }
      }

      /* === ADMIN STAT CARDS === */
      .admin-stat-card {
        background-color: #FFFFFF;
        border: 1px solid #E5E7EB;
        border-radius: 0.75rem;
        padding: 1.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        transition: transform 0.2s ease;
        display: flex;
        align-items: center;
        gap: 1rem;
      }

      .dark .admin-stat-card {
        background-color: #1F2937;
        border-color: #374151;
      }

      .admin-stat-card:hover {
        transform: translateY(-2px);
      }

      .admin-stat-icon {
        font-size: 2rem;
        flex-shrink: 0;
      }

      .admin-stat-content {
        flex: 1;
      }

      .admin-stat-value {
        font-size: 1.875rem;
        font-weight: 700;
        color: #1F2937;
        line-height: 1;
        margin-bottom: 0.25rem;
      }

      .dark .admin-stat-value {
        color: #F9FAFB;
      }

      .admin-stat-label {
        font-size: 0.875rem;
        color: #6B7280;
        line-height: 1.2;
      }

      .dark .admin-stat-label {
        color: #9CA3AF;
      }

      /* === ADMIN BADGES === */
      .admin-badge {
        display: inline-flex;
        align-items: center;
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 500;
        gap: 0.25rem;
      }

      .admin-badge-neu {
        background-color: #FEE2E2;
        color: #991B1B;
      }

      .dark .admin-badge-neu {
        background-color: rgba(127, 29, 29, 0.2);
        color: #FCA5A5;
      }

      .admin-badge-offen {
        background-color: #FEF3C7;
        color: #92400E;
      }

      .dark .admin-badge-offen {
        background-color: rgba(146, 64, 14, 0.2);
        color: #FCD34D;
      }

      .admin-badge-abgeschlossen {
        background-color: #DCFCE7;
        color: #166534;
      }

      .dark .admin-badge-abgeschlossen {
        background-color: rgba(20, 83, 45, 0.2);
        color: #86EFAC;
      }

      /* === ADMIN FILTER BUTTONS === */
      .admin-filter-container {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
      }

      .admin-filter-btn {
        padding: 0.5rem 1rem;
        border-radius: 0.5rem;
        font-size: 0.875rem;
        border: 1px solid #E5E7EB;
        background-color: #FFFFFF;
        color: #1F2937;
        cursor: pointer;
        transition: all 0.3s ease;
        white-space: nowrap;
      }

      .dark .admin-filter-btn {
        border-color: #374151;
        background-color: #1F2937;
        color: #F9FAFB;
      }

      .admin-filter-btn-active {
        background-color: #D2AE6C;
        color: white;
        border-color: #D2AE6C;
      }

      .admin-filter-btn:hover:not(.admin-filter-btn-active) {
        background-color: #F9FAFB;
      }

      .dark .admin-filter-btn:hover:not(.admin-filter-btn-active) {
        background-color: #374151;
      }

      /* === ADMIN TABLES === */
      .admin-table-container {
        overflow-x: auto;
      }

      .admin-table {
        width: 100%;
        border-collapse: collapse;
      }

      .admin-table th {
        background-color: #F9FAFB;
        padding: 1rem 1.5rem;
        text-align: left;
        font-weight: 600;
        color: #1F2937;
        border-bottom: 1px solid #E5E7EB;
        font-size: 0.875rem;
      }

      .dark .admin-table th {
        background-color: #374151;
        color: #F9FAFB;
        border-color: #4B5563;
      }

      .admin-table td {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid #E5E7EB;
        color: #1F2937;
      }

      .dark .admin-table td {
        border-color: #374151;
        color: #F9FAFB;
      }

      .admin-table tr:hover {
        background-color: #F9FAFB;
      }

      .dark .admin-table tr:hover {
        background-color: #374151;
      }

      /* === ADMIN MODAL === */
      .admin-modal-overlay {
        position: fixed;
        inset: 0;
        background-color: rgba(0, 0, 0, 0.5);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        z-index: 50;
      }

      .admin-modal-content {
        background-color: #FFFFFF;
        border-radius: 0.75rem;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        max-width: 42rem;
        width: 100%;
        max-height: 80vh;
        overflow-y: auto;
      }

      .dark .admin-modal-content {
        background-color: #1F2937;
      }

      .admin-modal-header {
        background-color: #F9FAFB;
        padding: 1.5rem;
        border-bottom: 1px solid #E5E7EB;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-radius: 0.75rem 0.75rem 0 0;
      }

      .dark .admin-modal-header {
        background-color: #374151;
        border-color: #4B5563;
      }

      .admin-modal-body {
        padding: 2rem;
      }

      /* === ADMIN FORM ELEMENTS === */
      .admin-label {
        display: block;
        font-size: 0.875rem;
        font-weight: 500;
        color: #374151;
        margin-bottom: 0.5rem;
      }

      .dark .admin-label {
        color: #D1D5DB;
      }

      .admin-text {
        color: #1F2937;
        padding: 0.5rem 0;
      }

      .dark .admin-text {
        color: #F9FAFB;
      }

      .admin-select {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 1px solid #E5E7EB;
        border-radius: 0.5rem;
        background-color: #FFFFFF;
        color: #1F2937;
        font-size: 0.875rem;
        transition: border-color 0.3s ease;
      }

      .dark .admin-select {
        border-color: #374151;
        background-color: #1F2937;
        color: #F9FAFB;
      }

      .admin-select:focus {
        outline: none;
        border-color: #D2AE6C;
        box-shadow: 0 0 0 3px rgba(210, 174, 108, 0.1);
      }

      .admin-textarea {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 1px solid #E5E7EB;
        border-radius: 0.5rem;
        background-color: #FFFFFF;
        color: #1F2937;
        font-size: 0.875rem;
        transition: border-color 0.3s ease;
        resize: vertical;
        min-height: 4rem;
      }

      .dark .admin-textarea {
        border-color: #374151;
        background-color: #1F2937;
        color: #F9FAFB;
      }

      .admin-textarea:focus {
        outline: none;
        border-color: #D2AE6C;
        box-shadow: 0 0 0 3px rgba(210, 174, 108, 0.1);
      }

      .admin-message-box {
        background-color: #F9FAFB;
        border: 1px solid #E5E7EB;
        border-radius: 0.5rem;
        padding: 1rem;
        color: #1F2937;
        line-height: 1.6;
        white-space: pre-wrap;
      }

      .dark .admin-message-box {
        background-color: #374151;
        border-color: #4B5563;
        color: #F9FAFB;
      }

      /* === RESPONSIVE ADMIN UTILITIES === */
      @media (max-width: 767px) {
        .admin-modal-content {
          margin: 1rem;
          max-width: calc(100vw - 2rem);
        }
        
        .admin-modal-header,
        .admin-modal-body {
          padding: 1rem;
        }
        
        .admin-filter-container {
          flex-direction: column;
        }
        
        .admin-filter-btn {
          width: 100%;
        }
        
        .admin-table-container {
          font-size: 0.875rem;
        }
        
        .admin-table th,
        .admin-table td {
          padding: 0.75rem 0.5rem;
        }
      }

      /* === ADMIN SUCCESS STATES === */
      .admin-alert-success {
        background-color: #F0FDF4;
        border: 1px solid #BBF7D0;
        color: #166534;
      }

      .dark .admin-alert-success {
        background-color: rgba(20, 83, 45, 0.2);
        border-color: #166534;
        color: #86EFAC;
      }
    </style>
  </body>
</html>